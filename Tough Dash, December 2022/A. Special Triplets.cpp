#include<bits/stdc++.h>
#define iamspeed ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL);
typedef long long ll;
using namespace std;
const ll N = 1010;
ll r, t, tt, n, m, k, i, x, y, j,cnt,cnt2, row, col, res, z, h, w, mx, mn, ans,sum;
string s,ss;
vector<ll>graph[N],vis(N);

int main()
{
    iamspeed


    cin>>n;
    cout<<"1 1 "<<n-2;

    return 0;
}