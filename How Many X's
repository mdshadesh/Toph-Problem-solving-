#include<bits/stdc++.h>
using namespace std;
int main()
{
   string s;
   cin>>s;
   int cnt=0,len;
   len = s.size();
   if(len<=100){
    for(int i=0;i<len;i++){
        if(s[i] == 'X'){
            cnt++;
        }
    }
    cout<<cnt<<endl;
   }
    return 0;
}